<form method="post" action="{{ path('app_secret_delete', {'id': secret.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ secret.id) }}">
    <button class="btn">Delete</button>
</form>
