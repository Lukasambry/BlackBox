<!DOCTYPE html>
<html lang="fr-FR">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            background-color: rgb(3, 7, 18);
            color: rgb(243, 244, 246);
        }
        .btn-primary {
            background-color: rgb(147, 51, 234);
            transition: background-color 0.2s;
        }
        .btn-primary:hover {
            background-color: rgb(126, 34, 206);
        }
        .card {
            background-color: rgb(17, 24, 39);
            border-radius: 0.75rem;
        }
    </style>
    {% block stylesheets %}{% endblock %}
    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>
<body class="min-h-screen bg-[#171e31]">
<nav>
    <div class="max-w-7xl mx-auto px-4">
        <div class="flex items-center justify-between h-20">
            {# Logo et Navigation principale #}
            <div class="flex items-center gap-8">
                <a href="{{ path('app_home') }}" class="flex items-center">
                    <img src="{{ asset('images/Logotype.png') }}" alt="Logo" class="h-9 w-auto">
                </a>
                
                {# Navigation principale #}
                <div class="hidden md:flex items-center gap-6">
                    <a href="{{ path('app_home') }}" class="text-gray-400 hover:text-white transition-colors">
                        <div class="flex items-center gap-2">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
                            </svg>
                            <span>Accueil</span>
                        </div>
                    </a>
                    {% if app.user %}
                        <a href="#" class="text-gray-400 hover:text-white transition-colors">
                            <div class="flex items-center gap-2">
                                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"/>
                                </svg>
                                <span>Sessions</span>
                            </div>
                        </a>
                    {% endif %}
                </div>
            </div>

            {# Partie droite - Recherche et Profil #}
            <div class="flex items-center gap-4">
                {# Barre de recherche #}
                <div class="hidden md:flex items-center">
                    <div class="relative">
                        <input type="text" 
                               placeholder="Rechercher..." 
                               class="w-64 bg-neutral-800 text-white placeholder-gray-400 px-4 py-2 pr-8 rounded-lg focus:outline-none focus:ring-2 focus:ring-[#5d35d8]">
                        <div class="absolute right-3 top-2.5 text-gray-400">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
                            </svg>
                        </div>
                    </div>
                </div>

                {# Menu utilisateur #}
{% if app.user %}
    <div class="flex items-center gap-4">
        <div class="relative group">
            <a href="{{ path('app_profile_show') }}" 
               class="flex items-center gap-3 bg-neutral-800 hover:bg-neutral-700 px-4 py-2 rounded-lg transition-colors">
                <div class="w-8 h-8 bg-[#5d35d8] rounded-full flex items-center justify-center">
                    <span class="text-sm font-bold">{{ app.user.nickname|slice(0, 1)|upper }}</span>
                </div>
                <span class="hidden md:inline">{{ app.user.nickname }}</span>
                <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                </svg>
            </a>

            {# Menu déroulant #}
            <div class="absolute right-0 mt-2 w-48 bg-neutral-800 rounded-lg shadow-lg py-1 hidden group-hover:block">
                <a href="{{ path('app_profile_show') }}" class="block px-4 py-2 text-sm text-gray-300 hover:bg-neutral-700 hover:text-white">
                    Mon profil
                </a>
                {% if is_granted('ROLE_ADMIN') %}
                    <a href="{{ path('app_admin') }}" class="block px-4 py-2 text-sm text-gray-300 hover:bg-neutral-700 hover:text-white">
                        Administration
                    </a>
                {% endif %}
                <div class="border-t border-neutral-700 my-1"></div>
                <a href="{{ path('app_logout') }}" class="block px-4 py-2 text-sm text-red-400 hover:bg-neutral-700 hover:text-red-300">
                    Se déconnecter
                </a>
                </div>
            </div>
            
            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('app_admin') }}" 
                class="text-gray-400 hover:text-white transition-colors">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/>
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
                    </svg>
                </a>
            {% endif %}
    </div>
{% else %}
    <a href="{{ path('app_login') }}" class="text-gray-300 hover:text-white transition-colors">Connexion</a>
    <a href="{{ path('app_register') }}" class="bg-[#5d35d8] hover:bg-[#4c2bb0] px-4 py-2 rounded-lg transition-colors">
        Inscription
    </a>
{% endif %}
            </div>
        </div>
    </div>
</nav>

<main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    {% for message in app.flashes('success') %}
        <div class="bg-green-600 text-white px-4 py-3 rounded-lg mb-4">
            {{ message }}
        </div>
    {% endfor %}

    {% for message in app.flashes('error') %}
        <div class="bg-red-600 text-white px-4 py-3 rounded-lg mb-4">
            {{ message }}
        </div>
    {% endfor %}

    {% block body %}{% endblock %}
</main>
</body>
</html>